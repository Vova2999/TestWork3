<Window x:Class="App.Views.Main.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:behaviors="clr-namespace:App.Wpf.Common.Behaviors;assembly=App.Wpf.Common"
		xmlns:main="clr-namespace:App.Views.Main"
		Width="800"
		Height="400"
		FontSize="14"
		Title="Редактор переменных окружения"
		WindowStartupLocation="CenterScreen"
		mc:Ignorable="d"
		d:DataContext="{DynamicResource DemoModel}">

	<Window.Resources>
		<main:DemoModel x:Key="DemoModel" />
	</Window.Resources>

	<b:Interaction.Behaviors>
		<behaviors:DragMoveBehavior />
	</b:Interaction.Behaviors>

	<b:Interaction.Triggers>
		<b:EventTrigger EventName="Loaded">
			<b:InvokeCommandAction Command="{Binding LoadedCommand, Mode=OneWay}" />
		</b:EventTrigger>
	</b:Interaction.Triggers>

	<Grid>
		<DataGrid AutoGenerateColumns="False"
				  VerticalScrollBarVisibility="Auto"
				  HorizontalScrollBarVisibility="Disabled"
				  ItemsSource="{Binding EnvironmentVariables, Mode=TwoWay}">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Ключ" Binding="{Binding Key, Mode=TwoWay}" Width="300" />
				<DataGridTextColumn Header="Значение" Binding="{Binding Value, Mode=TwoWay}" Width="*" />
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Window>